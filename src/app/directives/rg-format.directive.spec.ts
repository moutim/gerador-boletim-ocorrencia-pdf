import { RgFormatDirective } from './rg-format.directive';

describe('RgFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new RgFormatDirective();
    expect(directive).toBeTruthy();
  });
});
